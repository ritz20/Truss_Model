#include "node.h"

//// representation of a 2D truss

/*
 * reresented as a data structure that points to a node that points to a member that points to a node and so on
 * need to include 2 supports some how
 *
 */
class truss
{
public:

    // constructors
    truss();

    // setters

    // getters

    // member functions

    // imperatives
        /*
         * 1) must have 2 supports. Roller and Pin is assumed
         */

private:

    struct


    std::vector<double> dimentions;
    std::size_t num_nodes;
    std::size_t num_members;


};





















